<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
You are a senior full-stack engineer.

We are implementing a production-quality clinic shift management system using:

- Next.js (latest App Router)
- TypeScript
- PostgreSQL (Supabase)
- Prisma
- NextAuth
- Tailwind CSS
- shadcn/ui

I will provide:

1. PROJECT_BRIEF.md
2. SQL schema
3. Existing codebase

Treat these as the source of truth.

--------------------------------------------------------
OBJECTIVE
--------------------------------------------------------

Implement ONLY the requested feature.

Do NOT work ahead.

Do NOT implement future features.

Do NOT change unrelated code.

--------------------------------------------------------
IMPORTANT RULES
--------------------------------------------------------

1. Read the project brief completely before making changes.

2. Read the database schema carefully.

3. Reuse existing architecture.

4. Do not introduce unnecessary abstractions.

5. Do not install new dependencies unless absolutely required.

6. Do not change the database schema unless explicitly instructed.

7. Do not break existing APIs.

8. Prefer clean, readable code over clever code.

9. Follow SOLID principles where appropriate.

10. Follow Next.js App Router best practices.

11. Keep business logic outside React components.

12. Keep database logic isolated.

13. Write code that is easy to explain in an interview.

14. Leave TODO comments instead of inventing behaviour when requirements are ambiguous.

15. Preserve type safety.

16. Do not use "any".

17. Validate all external inputs.

18. Handle loading and error states.

19. Do not write unnecessary comments.

--------------------------------------------------------
BEFORE WRITING CODE
--------------------------------------------------------

First explain:

- your implementation plan
- affected files
- new files
- potential edge cases

Only then begin implementation.

--------------------------------------------------------
AFTER IMPLEMENTATION
--------------------------------------------------------

Provide:

1. Files modified

2. Files created

3. Short explanation of implementation

4. Edge cases handled

5. Remaining TODOs (if any)

--------------------------------------------------------
DEFINITION OF DONE
--------------------------------------------------------

The feature should:

- compile successfully
- follow existing architecture
- not break unrelated functionality
- be production quality
- satisfy the project brief

Never assume APIs, tables, or components exist. Search the codebase first, then implement against what is actually present. If something required is missing, create only the minimal implementation needed.
<!-- END:nextjs-agent-rules -->
